name: Windows release

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/windows-release.yml"
      - "packaging/windows/**"
      - "recomp-port/**"
      - "recomp/**"
      - "patches/**"
      - "tools/**"
      - "docs/BUILDING.md"
      - "README.md"
      - "Makefile"

permissions:
  contents: read

jobs:
  build-launcher:
    runs-on: windows-latest
    steps:
      - name: Keep source files in LF format
        run: git config --global core.autocrlf false

      - name: Check out public source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Build ROM-free Windows setup
        shell: powershell
        run: >-
          .\packaging\windows\build_release.ps1
          -Version "0.1.6-alpha"
          -BuildId "${{ github.sha }}"

      - name: Verify embedded public payload
        shell: powershell
        run: |
          $result = Join-Path $env:RUNNER_TEMP "windows-launcher-diagnostics.txt"
          $process = Start-Process `
              -FilePath .\dist\windows\40-Winks-PC-Port-Windows-x64.exe `
              -ArgumentList @("--diagnostics", "--diagnostics-output", "`"$result`"") `
              -PassThru
          $process.WaitForExit()
          if ($process.ExitCode -ne 0) {
              if (Test-Path $result) { Get-Content $result }
              throw "Windows launcher diagnostics failed."
          }
          Get-Content $result

      - name: Verify portable tool installation
        timeout-minutes: 15
        shell: powershell
        run: |
          $result = Join-Path $env:RUNNER_TEMP "windows-portable-tools.txt"
          $process = Start-Process `
              -FilePath .\dist\windows\40-Winks-PC-Port-Windows-x64.exe `
              -ArgumentList @("--install-portable-tools", "--diagnostics-output", "`"$result`"") `
              -PassThru
          $process.WaitForExit()
          if ($process.ExitCode -ne 0) {
              if (Test-Path $result) { Get-Content $result }
              throw "Self-managed Windows tool download failed."
          }
          Get-Content $result

      - name: Verify native build-tool discovery
        timeout-minutes: 10
        shell: powershell
        run: |
          $result = Join-Path $env:RUNNER_TEMP "windows-requirements-diagnostics.txt"
          $process = Start-Process `
              -FilePath .\dist\windows\40-Winks-PC-Port-Windows-x64.exe `
              -ArgumentList @("--requirements-check", "--diagnostics-output", "`"$result`"") `
              -PassThru
          $process.WaitForExit()
          if ($process.ExitCode -ne 0) {
              if (Test-Path $result) { Get-Content $result }
              throw "Native Windows build-tool discovery failed."
          }
          Get-Content $result

      - name: Compile and run with managed toolchain
        timeout-minutes: 5
        shell: powershell
        run: |
          $result = Join-Path $env:RUNNER_TEMP "windows-toolchain-smoke-test.txt"
          $process = Start-Process `
              -FilePath .\dist\windows\40-Winks-PC-Port-Windows-x64.exe `
              -ArgumentList @("--toolchain-smoke-test", "--diagnostics-output", "`"$result`"") `
              -PassThru
          $process.WaitForExit()
          if ($process.ExitCode -ne 0) {
              if (Test-Path $result) { Get-Content $result }
              throw "Managed Windows compiler smoke test failed."
          }
          Get-Content $result

      - name: Upload Windows setup
        uses: actions/upload-artifact@v4
        with:
          name: 40-Winks-PC-Port-Windows-x64
          path: |
            dist/windows/40-Winks-PC-Port-Windows-x64.exe
            dist/windows/40-Winks-PC-Port-Windows-x64.exe.sha256
          if-no-files-found: error
